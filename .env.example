# Copy to .env and adjust

# Python backend (existing) — embeddings loaded once on startup
PYTHON_BACKEND_URL=http://127.0.0.1:5000
MODEL=whisper_contrastive
DATASET=base

# Node server port (WebSocket + HTTP on the same port)
PORT=3001
